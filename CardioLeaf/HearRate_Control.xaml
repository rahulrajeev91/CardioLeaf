<UserControl x:Class="CardioLeaf.HearRate_Control"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Initialized="UserControl_Initialized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="150"/>
        </Grid.RowDefinitions>
        <WindowsFormsHost Margin="0,0,0,0" x:Name="ChartHost" Grid.Row="0" />
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" VerticalContentAlignment="Bottom" Margin="0,0,0,0" Background="White">
            <TextBlock   x:Name="debugText" TextWrapping="Wrap"  Background="White" Foreground="#FF515151" Margin="10,10,10,10"/>
        </ScrollViewer>

    </Grid>
</UserControl>
